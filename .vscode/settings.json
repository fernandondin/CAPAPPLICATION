{
    "sqltools.connections": [
        {
            "previewLimit": 50,
            "driver": "SQLite",
            "name": "Header",
            "database": "${workspaceFolder:CAPAPPLICATION}/Header.sqlite"
        }
    ]
}